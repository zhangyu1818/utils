import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'utils',
  outputPath: 'docs-dist',
  publicPath: 'https://zhangyu1818.github.io/utils/',
  base: '/utils/',
  exportStatic: {},
});
