{
  "name": "utils",
  "private": true,
  "scripts": {
    "start": "dumi dev",
    "build": "father-build",
    "bootstrap": "lerna bootstrap",
    "publish": "yarn run build && lerna publish"
  },
  "devDependencies": {
    "@umijs/fabric": "^2.2.2",
    "dumi": "^1.0.34",
    "father-build": "^1.18.4",
    "lerna": "^3.22.1",
    "prettier": "^2.1.2"
  }
}
